{
  "name": "ai-meeting-notes-summarizer",
  "version": "1.0.0",
  "description": "Full-stack MERN application for AI-powered meeting notes summarization",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "echo 'No build needed for backend-only deployment'",
    "start": "cd server && npm start",
    "install-server": "cd server && npm install",
    "install-client": "cd client && npm install",
    "install-all": "npm run install-server && npm run install-client"
  },
  "keywords": ["AI", "summarizer", "meeting notes", "MERN"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
